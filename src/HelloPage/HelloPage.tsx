import { Box } from "@material-ui/core";
import React from "react";

interface Props {}

const HelloPage: React.FC<Props> = (props: Props) => {
  return <Box className="HelloPage">hello</Box>;
};

export default HelloPage;
