import { Box } from "@material-ui/core";
import React from "react";

interface Props {}

const TopPage: React.FC<Props> = (props: Props) => {
  return <Box className="TopPage">Top</Box>;
};

export default TopPage;
